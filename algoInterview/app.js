// Write a JavaScript function to find the most frequent character in a given string.
// Test Data:
// ("Madam") -> "a"
// ("civic") -> "c"
// ("HellloL223LLL") -> "L"
// (12321) -> "It must be a string."

function freqChar(string) {
  let result;
  let charcters = {};
  for (var i = 0; i <= string.length; i++) {
    charcters[string[i]];
  }
}
